/*global jQuery */

(function (global, $) {
    "use strict";

    var Utl = global.Utl = global.Utl || {};

    /**
     * Get the property of the object.
     *
     * @param obj
     * @param attr
     * @param defaultValue
     */
    Utl.getAttr = global.GS.getAttr;

}(typeof window !== 'undefined' ? window : this, jQuery));